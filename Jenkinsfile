pipeline {
  agent any
  stages {
    stage('Source') { // Get code
      steps {
        echo 'a short message'
        }
    }
    
    stage('Compile') { // Compile and do unit testing
      steps {
        echo 'trying'
      }
     }
  }
}
